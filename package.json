{
    "name": "cloudflare-proxy-worker",
    "version": "1.0.0",
    "description": "代理接口到国内的 Cloudflare Worker",
    "main": "src/index.mjs",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy"
    },
    "dependencies": {
        "@whatwg-node/server": "0.9"
    },
    "devDependencies": {
        "wrangler": "^3.0.0"
    }
}